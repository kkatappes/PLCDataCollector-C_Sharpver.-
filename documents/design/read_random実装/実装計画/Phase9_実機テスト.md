# Phase9: 実機テスト

## ステータス
⏳ **未着手** - Phase1-8完了後に実施予定

## 概要
PLC実機環境でReadRandom(0x0403)の動作確認、パフォーマンス測定、バグ修正を行います。

## 前提条件
- ✅ Phase1-8完了: 全機能実装・単体テスト・統合テスト完了
- ✅ PLC実機環境が利用可能

## 実機テスト環境
- **PLC機種**: 三菱電機 Q00UDECPUまたは互換機種
- **接続方式**: Ethernet（UDP）
- **PLC IP**: 172.30.40.15
- **PLC Port**: 8192
- **フレームタイプ**: 4E
- **タイムアウト**: 500ms
- **テストプロジェクト**: PlcRealDeviceTest

---

## 実装ステップ

### ステップ26: 実機テストプログラムの作成

#### 実装対象
`PlcRealDeviceTest/Program.cs`

#### 実装内容

**⚠️ 重要: 実機テストの方針**
実機テストでは修正ができないため、**ハードコードされた既知の送信フレーム**を使用します。
- 接続設定: memo.mdに記載の実際の接続設定を使用
- 送信フレーム: memo.mdに記載の213バイトのフレームをハードコード
- 検証内容: レスポンスが正しく解析できることを確認

1. **ハードコード化されたテストコード**
   - 送信フレーム: 213バイトの固定データ（4Eフレーム）
   - 受信解析: SlmpDataParser.ParseReadRandomResponse()が正常動作するか確認
   - 結果の検証・表示

2. **設定ファイル作成**
   - `PlcRealDeviceTest/appsettings.json`
   - 実機のIPアドレス、ポート番号等を設定（memo.mdの設定を使用）

3. **テスト結果の記録**
   - コンソール出力
   - レスポンスHEX表示
   - デバイス値詳細表示

#### 実装コード（サンプル）

```csharp
using Andon.Utilities;
using System.Net;
using System.Net.Sockets;

namespace PlcRealDeviceTest;

class Program
{
    static async Task Main(string[] args)
    {
        Console.WriteLine("================================================================================");
        Console.WriteLine("PlcRealDeviceTest - ハードコード化フレーム送信・レスポンス解析テスト");
        Console.WriteLine("================================================================================");
        Console.WriteLine();

        // memo.mdに記載の接続設定（ハードコード）
        const string plcIp = "172.30.40.15";
        const int plcPort = 8192;
        const int timeout = 500;

        // memo.mdに記載の送信フレーム（213バイト、ハードコード）
        byte[] sendFrame = new byte[]
        {
            
        };

        Console.WriteLine("【接続設定】");
        Console.WriteLine($"  PLC IP       : {plcIp}");
        Console.WriteLine($"  PLC Port     : {plcPort}");
        Console.WriteLine($"  プロトコル   : UDP");
        Console.WriteLine($"  タイムアウト : {timeout}ms");
        Console.WriteLine();

        Console.WriteLine("【送信フレーム】");
        Console.WriteLine($"  長さ: {sendFrame.Length}バイト（期待値: 213バイト）");
        PrintHexDump(sendFrame);
        Console.WriteLine();

        try
        {
            using var udpClient = new UdpClient();
            udpClient.Client.ReceiveTimeout = timeout;

            Console.WriteLine($"[接続] {plcIp}:{plcPort} へ接続中...");
            var endpoint = new IPEndPoint(IPAddress.Parse(plcIp), plcPort);

            // フレーム送信
            Console.WriteLine($"[送信] データ送信中 ({sendFrame.Length}バイト)...");
            await udpClient.SendAsync(sendFrame, sendFrame.Length, endpoint);
            Console.WriteLine($"[確認] ✓ 送信完了 ({sendFrame.Length}バイト)");
            Console.WriteLine();

            // レスポンス受信
            Console.WriteLine($"[受信] レスポンス受信中（タイムアウト: {timeout}ms）...");
            var result = await udpClient.ReceiveAsync();
            byte[] responseFrame = result.Buffer;
            Console.WriteLine($"[受信] 完了: {responseFrame.Length} バイト");
            Console.WriteLine();

            Console.WriteLine("【受信レスポンス】");
            Console.WriteLine($"  長さ: {responseFrame.Length}バイト（期待値: 111バイト）");
            PrintHexDump(responseFrame);
            Console.WriteLine();

            // フレーム解析
            Console.WriteLine("================================================================================");
            Console.WriteLine("[解析] SlmpDataParser によるレスポンス解析");
            Console.WriteLine("================================================================================");

            // 4Eフレーム検証
            if (responseFrame[0] == 0xD4 && responseFrame[1] == 0x00)
            {
                Console.WriteLine("[判定] フレームタイプ: 4E");
                Console.WriteLine($"[判定] サブヘッダ: 0x{responseFrame[0]:X2} 0x{responseFrame[1]:X2}");
                Console.WriteLine();

                // ヘッダー情報抽出
                ushort sequence = BitConverter.ToUInt16(responseFrame, 2);
                ushort dataLength = BitConverter.ToUInt16(responseFrame, 11);
                ushort endCode = BitConverter.ToUInt16(responseFrame, 13);

                Console.WriteLine("[解析] 4Eフレーム構造:");
                Console.WriteLine($"  シーケンス番号  : 0x{sequence:X4}");
                Console.WriteLine($"  データ長        : {dataLength}バイト");
                Console.WriteLine($"  終了コード      : 0x{endCode:X4} {(endCode == 0 ? "(正常)" : "(エラー)")}");
                Console.WriteLine();

                if (endCode == 0)
                {
                    // データ部抽出
                    int dataStart = 15;
                    int dataSize = dataLength - 2; // 終了コード分を除く
                    byte[] deviceData = new byte[dataSize];
                    Array.Copy(responseFrame, dataStart, deviceData, 0, dataSize);

                    Console.WriteLine($"[解析] デバイスデータ ({dataSize}バイト):");
                    PrintHexDump(deviceData);
                    Console.WriteLine();

                    // デバイス値表示
                    Console.WriteLine("[解析] デバイス値（ワード型、リトルエンディアン）:");
                    for (int i = 0; i < Math.Min(10, dataSize / 2); i++)
                    {
                        ushort value = BitConverter.ToUInt16(deviceData, i * 2);
                        Console.WriteLine($"  D {i,2}: 0x{value:X4} ({value}) [Byte: 0x{deviceData[i * 2]:X2} 0x{deviceData[i * 2 + 1]:X2}]");
                    }

                    Console.WriteLine();
                    Console.WriteLine("[完了] レスポンス解析が正常に完了しました");
                }
                else
                {
                    Console.WriteLine($"[エラー] PLCからエラーコードが返されました: 0x{endCode:X4}");
                }
            }
            else
            {
                Console.WriteLine($"[エラー] 未知のフレームタイプ: 0x{responseFrame[0]:X2} 0x{responseFrame[1]:X2}");
            }

            Console.WriteLine();
            Console.WriteLine("================================================================================");
            Console.WriteLine("[完了] プログラムが正常終了しました");
            Console.WriteLine("================================================================================");
        }
        catch (Exception ex)
        {
            Console.WriteLine();
            Console.WriteLine("================================================================================");
            Console.WriteLine("[エラー] 例外が発生しました");
            Console.WriteLine("================================================================================");
            Console.WriteLine($"[エラー内容] {ex.Message}");
            Console.WriteLine($"[スタックトレース]");
            Console.WriteLine(ex.StackTrace);
        }
    }

    /// <summary>
    /// HEXダンプ表示
    /// </summary>
    static void PrintHexDump(byte[] data)
    {
        for (int i = 0; i < data.Length; i += 16)
        {
            Console.Write($"  {i:X4}: ");

            // HEXバイト表示
            for (int j = 0; j < 16; j++)
            {
                if (i + j < data.Length)
                    Console.Write($"{data[i + j]:X2} ");
                else
                    Console.Write("   ");
            }

            Console.Write(" ");

            // ASCII文字表示
            for (int j = 0; j < 16; j++)
            {
                if (i + j < data.Length)
                {
                    byte b = data[i + j];
                    Console.Write((b >= 32 && b < 127) ? (char)b : '.');
                }
            }

            Console.WriteLine();
        }
    }

}
```

#### appsettings.json（実機用）

**注意**: このテストではappsettings.jsonは使用しません。全ての設定はコード内にハードコードされています。

---

### ステップ27: 実機での動作確認

#### 確認項目

**⚠️ 重要: 単純明快な検証内容**
実機テストは修正ができないため、以下の**単一テスト**のみを実施します。

1. **送信フレーム確認**
   - 送信フレーム長: 213バイト（ハードコード済み）
   - フレーム内容: memo.mdと同一（ハードコード済み）

2. **レスポンス受信確認**
   - 受信フレーム長: 111バイト（期待値）
   - 終了コード: 0x0000（正常）

3. **レスポンス解析確認**
   - 4Eフレームとして正しく解析できるか
   - デバイスデータ部が正しく抽出できるか
   - デバイス値が正しく表示されるか

#### 実機テスト手順

1. **環境準備**
   ```bash
   cd PlcRealDeviceTest
   dotnet restore
   dotnet build
   ```

2. **テスト実行**
   ```bash
   dotnet run
   ```

3. **結果確認**
   - コンソール出力を確認
   - 送信フレーム: 213バイトのHEXダンプ表示
   - 受信フレーム: 111バイトのHEXダンプ表示
   - 解析結果: デバイス値の詳細表示

4. **成功条件**
   - ✅ UDP通信が成功
   - ✅ レスポンス受信成功（111バイト）
   - ✅ 終了コード: 0x0000
   - ✅ デバイス値が正しく表示される

5. **失敗時の対応**
   - タイムアウト発生 → ネットワーク設定確認
   - エラーコード発生 → PLCの状態確認
   - 解析エラー → フレーム構造の再確認

---

### ステップ28: 結果記録と次フェーズへの移行

#### 実機テスト結果の記録

1. **成功時**
   - コンソール出力をファイル保存
   - 送信フレームと受信フレームのHEXダンプを保存
   - デバイス値の解析結果を保存
   - `documents/implementation_records/execution_logs/phase9_real_device_test_success.log`に保存

2. **失敗時**
   - エラーメッセージとスタックトレースを保存
   - 失敗箇所の特定
   - `documents/implementation_records/execution_logs/phase9_real_device_test_failure.log`に保存
   - 開発環境に戻って修正実施

#### バグ修正が必要な場合の対応

**重要**: 実機テストで問題が発見された場合は、開発環境に戻って修正します。

1. **問題の特定**
   - エラーログから問題箇所を特定
   - 送受信フレームのHEXダンプから問題を分析

2. **開発環境での修正**
   - 単体テストで問題を再現
   - コード修正
   - 単体テスト・統合テストで検証

3. **再実機テスト**
   - 修正後、再度実機テストを実施
   - 成功するまで繰り返し

---

## 完了条件
- ✅ PlcRealDeviceTest実装完了（ハードコード化フレーム送信版）
- ✅ 実機環境でUDP通信成功
- ✅ 213バイトフレーム送信成功
- ✅ 111バイトレスポンス受信成功
- ✅ 4Eフレーム解析成功（終了コード: 0x0000）
- ✅ デバイス値表示成功
- ✅ テスト結果記録完了

## 次フェーズへの依存関係
- **Phase10**: 旧Read(0x0401)コードの削除・クリーンアップ（Phase12完了後に実施を推奨）
- **Phase12**: ProcessedDeviceRequestInfo再設計（🔴 **最優先** - 実機データ取得に必須）

## リスク管理
| リスク | 影響 | 対策 |
|--------|------|------|
| **ネットワーク不安定** | 高 | ・ハードコード化フレームで単純化<br>・タイムアウト設定: 500ms |
| **実機環境での修正不可** | 高 | ・既知の動作するフレームを使用<br>・memo.mdの実績データを利用 |
| **PLC機種依存の問題** | 中 | ・SLMP仕様書準拠を厳守<br>・4Eフレームで統一 |
| **実機環境へのアクセス制限** | 低 | ・テスト環境の事前確保<br>・スケジュール調整 |

---

**作成日**: 2025-11-18
**元ドキュメント**: read_to_readrandom_migration_plan.md
